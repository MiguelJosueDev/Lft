import { {{ _ModelName }}UpdateController } from 'features/{{ _moduleNameCamel }}/controllers';
import { useNavigate } from 'react-router-dom';
import { RoutePaths } from 'features/routing';

export const {{ _ModelName }}UpdateView = ({ {{ _modelNameCamel }}Id }) => {
  const parsedId =
    !isNaN(Number({{ _modelNameCamel }}Id)) && {{ _modelNameCamel }}Id !== '' ? Number({{ _modelNameCamel }}Id) : {{ _modelNameCamel }}Id;
  const navigate = useNavigate();

  return (
    <{{ _ModelName }}UpdateController.Modal
      isOpen={true}
      isDismissable={false}
      title="Update {{ _ModelName }}"
      {{ _modelNameCamel }}Id={parsedId}
      onSuccess={() => navigate(RoutePaths.{{ _moduleNameCamel }}.list())}
      onClose={() => navigate(RoutePaths.{{ _moduleNameCamel }}.list())}
    />
  );
};
